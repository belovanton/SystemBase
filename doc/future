* Записывать таблицу состояний в виде
		$table = array(
			array(0, 0, 0, 0, 0, 0, 0, 0),
			array(0, 1, 1, 1, 1, 6, 2, 1)  // номера реакций
		);
* Двумерные вкладки: пересечение состояния и действия отображает определенную реакцию (идея Валента).
* Вторник.
* Среда.
* Вместо комментариев в прототипе нужно сразу писать тест-план. 
* Что, убийца?
